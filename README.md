# conll-2003-ner-team-19

Build state-of-the-art models in Named Entity Recognition using the CoNLL-2003 dataset.

## Creating Environment

Dependencies are stored in `environment.yml`. You can recreate the environment in Conda with the following commands:

```
conda env create -f environment.yml
conda activate conll2003-eda
```